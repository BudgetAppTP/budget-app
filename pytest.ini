[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers
markers =
    unit: fast tests for pure business logic
    integration: Flask/database/API integration tests
    security: authentication and authorization regression tests
