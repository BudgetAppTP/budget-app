FLASK_ENV=development
APP_ENV=development
SECRET_KEY=change-me
WTF_CSRF_ENABLED=true
DEBUG=true
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=pass1234
IMPORT_XLSX_PATH=/mnt/data/BugetAppTP.xlsx
DEFAULT_CURRENCY=EUR

# Optional email verification delivery.
# When SMTP is not configured, verification codes are logged by the backend.
SMTP_SERVER=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# Optional Google OAuth backend audience check.
# Must match VITE_GOOGLE_CLIENT_ID in client/.env when Google login is enabled.
GOOGLE_CLIENT_ID=
